{{ pillar['headers']['auto_generated'] }}
{{ pillar['headers']['warning'] }}
SHELL=/bin/bash
PATH=/usr/local/sbin:/usr/local/bin:/sbin:/bin:/usr/sbin:/usr/bin

{%- for job in jobs %}
{{ job.cronexpr }} root {{ job.scriptfile }} >> {{ logdir }}/restic.log 2>&1
{%- endfor %}
